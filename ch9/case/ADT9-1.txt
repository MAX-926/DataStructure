抽象数据类型queue
{
    实例：
        元素的有序表，一端称为队首，一端称为队尾
    操作：
        empty():判断队列是否为空。返回true，当且仅当队列为空
        size():返回队列中元素的个数
        pop():将元素从队首弹出(删除队首元素)
        push(x):将元素x从队尾压入
        front():获取队首元素
        back():获取队尾元素
};