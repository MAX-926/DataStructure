抽象数据结构栈
抽象数据类型stack
{
    方法：
        empty();//栈为空时返回true，否则返回false
        size();//返回栈中元素个数
        push(x);//将元素x压入栈顶
        pop();//删除栈顶元素
        top();//返回栈顶元素
    数据：
        linearList;一端称为顶，一端称为低
}